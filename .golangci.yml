version: "2"

linters:
  default: none
  enable:
    - errcheck
    - gosec
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
  settings:
    gosec:
      excludes:
        - G104

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
