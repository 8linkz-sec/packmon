version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gofumpt
    - gosec
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

linters-settings:
  gofumpt:
    extra-rules: true
  gosec:
    excludes:
      - G104
