replicaCount: 1

image:
  repository: ghcr.io/8linkz/packmon-server
  tag: "0.5.0"

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: packmon.example.internal
      paths:
        - path: /
          pathType: Prefix

admin:
  initialPassword: replace-in-rancher-secret

server:
  publicHost: packmon.example.internal
  trustedProxies:
    - 10.42.0.0/16
  encryptionKey: replace-in-rancher-secret

metrics:
  host: 127.0.0.1
  port: 9090

postgresql:
  enabled: true
  image: postgres:18-alpine
  storageClassName: local-path
  size: 20Gi

backup:
  enabled: true
  image: postgres:18-alpine
  storageClassName: local-path
  size: 20Gi
  schedule: "0 3 * * *"
  retentionDays: 7

feeds:
  socketEnabled: true
  socketApiKey: replace-me
  vulncheckEnabled: true
  vulncheckApiKey: replace-me
